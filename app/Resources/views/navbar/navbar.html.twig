<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">
                Chloris
            </a>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path('homepage') }}#about">About the site</a></li>
                <li><a href="{{ path('homepage') }}#contact">Contact</a></li>
                <li><a href="{{ path('homepage') }}#start">How to start</a></li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('admin') }}">Managament</a></li>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
                    {% if app.user.haveAnyPlantsNeedWatering %}
                        <li>
                            <a href="{{ path('myPlants') }}">
                                <img src="{{ asset('images/exclamation-mark.png') }}" alt="exclamation-mark">
                            </a>
                        </li>
                    {% endif %}
                    <li class="dropdown">
                        <a
                            href="#"
                            class="dropdown-toggle"
                            data-toggle="dropdown"
                            role="button"
                            aria-haspopup="true"
                            aria-expanded="false">
                                Account
                                <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('myPlants') }}">My plants</a></li>
                            <li><a href="{{ path('addPlant') }}">Add Plant</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li><a href="{{ path('fos_user_security_login') }}">Login</a></li>
                    <li><a href="{{ path('fos_user_registration_register') }}">Register</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
