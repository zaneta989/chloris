<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            {% if is_granted('ROLE_USER') %}
                <a class="navbar-brand" href="{{ path('myPlants') }}">Chloris</a>
            {% else %}
                <a class="navbar-brand" href="{{ path('homepage') }}">Chloris</a>
            {% endif %}
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav navbar-right">
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('admin') }}">Managament</a></li>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
                    {% if app.user.haveAnyPlantsNeedWatering %}
                        <li>
                            <a href="{{ path('myPlants') }}">
                                <img src="{{ asset('images/wateringCanToNavbar.png') }}" alt="watered your plants"
                                     title="You have some plants that you should watered!"
                                >
                            </a>
                        </li>
                    {% endif %}
                    <li class="dropdown">
                        <a
                                href="#"
                                class="dropdown-toggle"
                                data-toggle="dropdown"
                                role="button"
                                aria-haspopup="true"
                                aria-expanded="false">
                            Account
                            <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('myPlants') }}">My plants</a></li>
                            <li><a href="{{ path('addPlant') }}">Add Plant</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ path('fos_user_security_logout') }}">
                                    <span class="glyphicon glyphicon-log-out" title="log out"></span> Logout
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li><a href="{{ path('homepage') }}#about">About the site</a></li>
                    <li><a href="{{ path('homepage') }}#contact">Contact</a></li>
                    <li><a href="{{ path('homepage') }}#start">How to start</a></li>
                    <li><a href="{{ path('fos_user_registration_register') }}">Register</a></li>
                    <li><a href="{{ path('fos_user_security_login') }}">
                            <span class="glyphicon glyphicon-log-in" title="log in"></span> Login
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
